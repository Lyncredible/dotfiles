# Claude Code writes machine-local state here (sessions, caches, daemon and
# job dirs), and each release adds more. Ignore everything, then re-include
# only what this repo manages.
*

!.gitignore
!CLAUDE.md
!settings.json.dist

!agents/
!agents/**
!rules/
!rules/**
